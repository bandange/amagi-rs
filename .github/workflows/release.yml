# Build and release binaries for multiple platforms
name: Release

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  BUILD_TYPE: release
  BIN_NAME: amagi
  AMAGI_BUILD_GIT_HASH: ${{ github.sha }}

jobs:
  build:
    name: ${{ matrix.name }}
    permissions:
      contents: read
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64-gnu
            os: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            artifact: amagi-x86_64-unknown-linux-gnu.tar.gz
          - name: linux-aarch64-gnu
            os: ubuntu-24.04
            target: aarch64-unknown-linux-gnu
            artifact: amagi-aarch64-unknown-linux-gnu.tar.gz
            cross: true
          - name: linux-x86_64-musl
            os: ubuntu-24.04
            target: x86_64-unknown-linux-musl
            artifact: amagi-x86_64-unknown-linux-musl.tar.gz
            cross: true
          - name: linux-aarch64-musl
            os: ubuntu-24.04
            target: aarch64-unknown-linux-musl
            artifact: amagi-aarch64-unknown-linux-musl.tar.gz
            cross: true
          - name: macos-x86_64
            os: macos-26-intel
            target: x86_64-apple-darwin
            artifact: amagi-x86_64-apple-darwin.tar.gz
          - name: macos-aarch64
            os: macos-26
            target: aarch64-apple-darwin
            artifact: amagi-aarch64-apple-darwin.tar.gz
          - name: windows-x86_64-msvc
            os: windows-2025
            target: x86_64-pc-windows-msvc
            artifact: amagi-x86_64-pc-windows-msvc.zip

    steps:
      - uses: actions/checkout@v6.0.3

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        uses: taiki-e/install-action@v2.81.11
        with:
          tool: cross@0.2.5

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2.9.1

      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --locked --bin "$BIN_NAME" --target ${{ matrix.target }}
          else
            cargo build --release --locked --bin "$BIN_NAME" --target ${{ matrix.target }}
          fi

      - name: Verify runnable version metadata
        if: ${{ matrix.cross != true }}
        shell: bash
        run: |
          binary="target/${{ matrix.target }}/release/${BIN_NAME}"
          if [ "${{ runner.os }}" = "Windows" ]; then
            binary="${binary}.exe"
          fi

          version_output="$(AMAGI_LANG=en "${binary}" --version)"
          printf '%s\n' "${version_output}"
          grep -F "Release" <<<"${version_output}"
          grep -E "Version: [0-9]+\\.[0-9]+\\.[0-9]+ \\(built [0-9]{4}-[0-9]{2}-[0-9]{2}T" <<<"${version_output}"
          grep -E "Rustc: rustc [0-9]" <<<"${version_output}"
          grep -F "Target: ${{ matrix.target }}" <<<"${version_output}"

      - name: Verify cross build metadata strings
        if: ${{ matrix.cross == true }}
        shell: bash
        run: |
          binary="target/${{ matrix.target }}/release/${BIN_NAME}"
          strings "${binary}" > version-strings.txt
          grep -E "rustc [0-9]" version-strings.txt
          grep -E "[0-9]{4}-[0-9]{2}-[0-9]{2}T" version-strings.txt

      - name: Package (tar.gz)
        if: runner.os != 'Windows'
        run: tar -czvf ${{ matrix.artifact }} -C target/${{ matrix.target }}/release ${{ env.BIN_NAME }}

      - name: Package (zip)
        if: runner.os == 'Windows'
        run: Compress-Archive -Path target/${{ matrix.target }}/release/${{ env.BIN_NAME }}.exe -DestinationPath ${{ matrix.artifact }}

      - name: Upload
        uses: actions/upload-artifact@v7.0.1
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    permissions:
      contents: write
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v8.0.1
        with:
          path: dist
          merge-multiple: true

      - name: Release
        uses: softprops/action-gh-release@v3.0.0
        with:
          files: dist/*
          generate_release_notes: true
